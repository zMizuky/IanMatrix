# Changelog

The `linkita` branch contains the latest changes.
The [v4 branch](v4.md) contains the latest stable version.

To switch to the `linkita` branch, run:

```sh
git submodule set-branch --branch linkita themes/linkita
git submodule update --remote themes/linkita
```

## [v5](https://codeberg.org/salif/linkita/compare/v4..linkita) (unreleased)
